// Shared variables.
let _APP = {
    // Holds the server/client config. 
    config: null,
    // Holds a cache of the server DB commands. 
    commands: [],
    // Holds the current appView.
    appView: "",
};
